{
  "name": "status-checker",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild ./src/functions/**/*.ts --bundle --outdir=dist --target=node20 --platform=node"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2"
  }
}
