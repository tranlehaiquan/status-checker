import React from 'react';

interface Props {
  className?: string;
};

const StatusCheckResults: React.FC<Props> = (props) => {
  return (
    <div>
      
    </div>
  );
}

export default StatusCheckResults;